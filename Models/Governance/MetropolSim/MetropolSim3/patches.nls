
;;;;;;;;;;;;
;; patches procedures
;;;;;;;;;;;;


to-report patch-with-number [n]
  let x (floor (n / world-height) + min-pxcor) let y (n - (floor (n / world-height))* world-height + min-pycor)
  report patch x y
end
