
;; temp file for list util functions (no submodule push/pull available)

to-report list-times-element-wise [l1 l2]
  let res []
  let i 0
  foreach l1 [
    set res lput (? * (item i l2)) res
    set i i + 1 
  ]
  report res
end

