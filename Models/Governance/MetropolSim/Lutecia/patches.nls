
;;;;;;;;;;;;
;; patches procedures
;;;;;;;;;;;;


to-report patch-with-number [num]
  let x (floor (num / world-height) + min-pxcor) let y (num - (floor (num / world-height))* world-height + min-pycor)
  report patch x y
end
