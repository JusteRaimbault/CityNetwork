# progress of the current experiment using log size
# -- total experiment size is static -> option to read it from exp.oml ?
echo "("`cat log_exp | wc -l`" - 33 )* 100 /(100 * 11 * 3 * 11 * 3)" | bc -l

